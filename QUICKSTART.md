# 🚀 快速入門指南

## 5 分鐘快速體驗

### 步驟 1: 測試系統

```bash
./test_system.sh
```

或使用 Makefile:

```bash
make test
```

### 步驟 2: 啟動伺服器

開啟**終端機 1**：

```bash
./start_servers.sh
```

或

```bash
make start-server
```

保持這個終端機運行。

### 步驟 3: 上傳遊戲（開發者）

開啟**終端機 2**：

```bash
./start_developer_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`dev1`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `1` (上傳新遊戲)
7. 選擇 `1` (tictactoe - 井字遊戲)
8. 輸入 `y` 確認上傳
9. 等待上傳完成

### 步驟 4: 玩家 1 建立房間

開啟**終端機 3**：

```bash
./start_player_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`player1`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `1` (瀏覽遊戲商城) - 確認遊戲已上傳
7. 按 Enter 繼續
8. 選擇 `4` (建立房間)
9. 選擇 `1` (選擇 TicTacToe)
10. 輸入 `y` (下載遊戲)
11. 等待下載完成
12. 保持在房間等待畫面

### 步驟 5: 玩家 2 加入房間

開啟**終端機 4**：

```bash
./start_player_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`player2`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `5` (加入房間)
7. 選擇 `1` (選擇房間 1)
8. 等待房主開始遊戲

### 步驟 6: 開始遊戲

回到**終端機 3** (player1)：
1. 選擇 `1` (開始遊戲)
2. 遊戲會自動啟動

**終端機 3** 和**終端機 4** 都會顯示井字遊戲介面，開始遊玩！

---

## 測試不同遊戲類型

### CLI 遊戲 - 井字遊戲
- 雙人對戰
- 文字介面
- 按照上述步驟操作

### GUI 遊戲 - 猜數字對戰
1. 開發者上傳 `number_guess`
2. 玩家下載並建立房間
3. 會出現圖形介面視窗

### 多人遊戲 - 石頭剪刀布
1. 開發者上傳 `rock_paper_scissors`
2. 需要至少 **3 位玩家**
3. 開啟 3-5 個玩家客戶端並加入同一房間
4. 房主開始遊戲

---

## 常用命令

### 使用 Shell 腳本

```bash
# 啟動伺服器
./start_servers.sh

# 啟動開發者客戶端
./start_developer_client.sh

# 啟動玩家客戶端
./start_player_client.sh

# 停止伺服器
./stop_servers.sh

# 測試系統
./test_system.sh
```

### 使用 Makefile

```bash
# 查看幫助
make help

# 測試系統
make test

# 啟動伺服器
make start-server

# 啟動開發者客戶端
make start-dev

# 啟動玩家客戶端
make start-player

# 停止伺服器
make stop

# 清理資料
make clean
```

---

## 連線到遠端伺服器

如果伺服器部署在遠端機器上：

```bash
# 啟動開發者客戶端並連線到遠端
./start_developer_client.sh <server_ip> 6001

# 啟動玩家客戶端並連線到遠端
./start_player_client.sh <server_ip> 6002
```

例如：
```bash
./start_developer_client.sh 140.113.123.45 6001
./start_player_client.sh 140.113.123.45 6002
```

---

## 故障排除

### 埠口被佔用

```bash
# 查看佔用埠口的程序
lsof -i :6001
lsof -i :6002

# 停止伺服器
./stop_servers.sh
```

### GUI 遊戲無法啟動

```bash
# Ubuntu/Debian 安裝 tkinter
sudo apt-get install python3-tk

# 測試 tkinter
python3 -c "import tkinter; print('tkinter OK')"
```

### 清理並重新開始

```bash
# 清理所有資料
make clean

# 重新啟動伺服器
./start_servers.sh
```

---

## 下一步

- 閱讀完整的 [README.md](README.md)
- 嘗試上傳所有三款遊戲
- 測試遊戲更新功能
- 嘗試評分系統
- 開發自己的遊戲！

---

Have fun! 🎮
