# 🚀 快速入門指南

本專案使用 `uv` 進行依賴管理與環境執行。

## 前置準備

請先安裝 `uv`：

### macOS / Linux
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### Windows
```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
```

## 5 分鐘快速體驗

### 步驟 1: 初始化環境

首次執行時，`uv` 會自動建立虛擬環境並安裝依賴。

```bash
uv sync
```

### 步驟 2: 啟動伺服器

開啟**終端機 1**：

```bash
./start_servers.sh
```

或使用 `uv` 直接執行：

```bash
uv run python3 server/lobby_server.py
```

保持這個終端機運行。

### 步驟 3: 上傳遊戲（開發者）

開啟**終端機 2**：

```bash
./start_developer_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`dev1`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `1` (上傳新遊戲)
7. 選擇 `1` (tictactoe - 井字遊戲)
8. 輸入 `y` 確認上傳
9. 等待上傳完成

### 步驟 4: 玩家 1 建立房間

開啟**終端機 3**：

```bash
./start_player_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`player1`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `1` (瀏覽遊戲商城) - 確認遊戲已上傳
7. 按 Enter 繼續
8. 選擇 `4` (建立房間)
9. 選擇 `1` (選擇 TicTacToe)
10. 輸入 `y` (下載遊戲)
11. 等待下載完成
12. 保持在房間等待畫面

### 步驟 5: 玩家 2 加入房間

開啟**終端機 4**：

```bash
./start_player_client.sh
```

操作流程：
1. 選擇 `2` (註冊)
2. 輸入帳號：`player2`
3. 輸入密碼：`123456`
4. 選擇 `1` (登入)
5. 使用相同帳號密碼登入
6. 選擇 `5` (加入房間)
7. 選擇 `1` (選擇房間 1)
8. 等待房主開始遊戲

### 步驟 6: 開始遊戲

回到**終端機 3** (player1)：
1. 選擇 `1` (開始遊戲)
2. 遊戲會自動啟動

**終端機 3** 和**終端機 4** 都會顯示井字遊戲介面，開始遊玩！

---

## 專案結構

```
.
├── pyproject.toml          # uv 專案設定檔
├── uv.lock                 # 依賴鎖定檔 (執行 uv sync 後產生)
├── start_servers.sh        # 啟動伺服器腳本 (自動使用 uv)
├── start_developer_client.sh # 啟動開發者客戶端腳本 (自動使用 uv)
├── start_player_client.sh  # 啟動玩家客戶端腳本 (自動使用 uv)
├── server/                 # 伺服器程式碼
├── player/                 # 玩家客戶端程式碼
├── developer/              # 開發者客戶端程式碼
└── database/               # 資料庫檔案
```

## 開發說明

### 新增依賴

如果需要新增 Python 套件：

```bash
uv add <package_name>
```

### 執行測試

```bash
uv run pytest
```

## 連線到遠端伺服器

如果伺服器部署在遠端機器上：

```bash
# 啟動開發者客戶端並連線到遠端
./start_developer_client.sh <server_ip> 6001

# 啟動玩家客戶端並連線到遠端
./start_player_client.sh <server_ip> 6002
```

例如：
```bash
./start_developer_client.sh 140.113.123.45 6001
./start_player_client.sh 140.113.123.45 6002
```

---

## 故障排除

### 埠口被佔用

```bash
# 查看佔用埠口的程序
lsof -i :6001
lsof -i :6002

# 停止伺服器
./stop_servers.sh
```

### GUI 遊戲無法啟動

```bash
# Ubuntu/Debian 安裝 tkinter
sudo apt-get install python3-tk

# 測試 tkinter
uv run python3 -c "import tkinter; print('tkinter OK')"
```

---

Have fun! 🎮
